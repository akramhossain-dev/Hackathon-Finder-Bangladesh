/**
 * hooks/index.ts — Custom React hooks barrel export (Phase 1 scaffold).
 *
 * Phase 2+: add hooks here as they are implemented:
 *
 *   export { useAuth }         from "./useAuth";       // Phase 2
 *   export { useHackathons }   from "./useHackathons"; // Phase 3
 *   export { useBookmarks }    from "./useBookmarks";  // Phase 5
 *   export { useReminders }    from "./useReminders";  // Phase 5
 *
 * Hooks should be named useX and live in their own files.
 * Data-fetching hooks will use SWR or React Query (decision in Phase 3).
 */

export {};
