/**
 * components/shared/index.ts — Shared component barrel export (Phase 1 scaffold).
 *
 * Shared components are generic, reusable UI pieces used across multiple features.
 * They are NOT shadcn/ui base components (those live in components/ui/).
 *
 * Phase 3+: export shared components here:
 *
 *   export { default as PageHeader }      from "./PageHeader";
 *   export { default as EmptyState }      from "./EmptyState";
 *   export { default as LoadingSpinner }  from "./LoadingSpinner";
 *   export { default as ErrorBoundary }   from "./ErrorBoundary";
 *   export { default as Pagination }      from "./Pagination";
 *   export { default as Badge }           from "./Badge";
 *   export { default as StatusBadge }     from "./StatusBadge";
 */

export {};
