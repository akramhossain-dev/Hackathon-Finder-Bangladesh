/**
 * redis.ts — Redis / BullMQ connection placeholder (Phase 0).
 *
 * Phase 1+ implementation:
 *  1. npm install ioredis bullmq
 *  2. Add REDIS_URL to .env
 *  3. Create a shared IORedis connection instance
 *  4. Pass connection to BullMQ Queue / Worker constructors
 *  5. See docs/ARCHITECTURE.md §6 (Reminder System)
 */

// Phase 1+: export const redisConnection = new IORedis(env.REDIS_URL);
export {};
