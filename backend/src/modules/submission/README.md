# Module: submission

**Phase**: TBD  
**Status**: Scaffold only — no implementation yet.

## Folder structure (to be created in the implementing phase)

```
submission/
├── submission.model.ts       ← Mongoose schema + model
├── submission.routes.ts      ← Express Router
├── submission.controller.ts  ← thin request/response layer
├── submission.service.ts     ← business logic
├── submission.schema.ts      ← Zod validation schemas
└── submission.types.ts       ← TypeScript interfaces
```

## Notes

See `docs/` for architecture decisions and API contracts for this module.
