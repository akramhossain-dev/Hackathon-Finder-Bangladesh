# Module: reminder

**Phase**: TBD  
**Status**: Scaffold only — no implementation yet.

## Folder structure (to be created in the implementing phase)

```
reminder/
├── reminder.model.ts       ← Mongoose schema + model
├── reminder.routes.ts      ← Express Router
├── reminder.controller.ts  ← thin request/response layer
├── reminder.service.ts     ← business logic
├── reminder.schema.ts      ← Zod validation schemas
└── reminder.types.ts       ← TypeScript interfaces
```

## Notes

See `docs/` for architecture decisions and API contracts for this module.
