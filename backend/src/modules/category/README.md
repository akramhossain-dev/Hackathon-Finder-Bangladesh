# Module: category

**Phase**: TBD  
**Status**: Scaffold only — no implementation yet.

## Folder structure (to be created in the implementing phase)

```
category/
├── category.model.ts       ← Mongoose schema + model
├── category.routes.ts      ← Express Router
├── category.controller.ts  ← thin request/response layer
├── category.service.ts     ← business logic
├── category.schema.ts      ← Zod validation schemas
└── category.types.ts       ← TypeScript interfaces
```

## Notes

See `docs/` for architecture decisions and API contracts for this module.
