# Module: bookmark

**Phase**: TBD  
**Status**: Scaffold only — no implementation yet.

## Folder structure (to be created in the implementing phase)

```
bookmark/
├── bookmark.model.ts       ← Mongoose schema + model
├── bookmark.routes.ts      ← Express Router
├── bookmark.controller.ts  ← thin request/response layer
├── bookmark.service.ts     ← business logic
├── bookmark.schema.ts      ← Zod validation schemas
└── bookmark.types.ts       ← TypeScript interfaces
```

## Notes

See `docs/` for architecture decisions and API contracts for this module.
