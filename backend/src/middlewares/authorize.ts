/**
 * authorize.ts — Role-based access control middleware (Phase 0 stub).
 *
 * Phase 1 implementation:
 *  1. Accept allowed roles array: authorize(['admin'])
 *  2. Check req.user.role (set by authenticate middleware)
 *  3. Throw ApiError.forbidden() if role not in allowed list
 *
 * See docs/AUTH.md for role definitions.
 */

// Phase 1:
// import { UserRole } from "../types";
// export const authorize = (roles: UserRole[]) => (req, res, next) => { ... };
export {};
