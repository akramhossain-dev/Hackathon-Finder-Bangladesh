/**
 * rateLimiter.ts — Rate limiting middleware (Phase 0 stub).
 *
 * Phase 1 implementation:
 *  1. npm install express-rate-limit
 *  2. Create limiter for auth routes (stricter) and public routes (looser)
 *  3. Apply auth limiter to /api/v1/auth/* routes
 *
 * See docs/SECURITY.md for rate limit strategy.
 */

// Phase 1:
// import rateLimit from "express-rate-limit";
// export const authRateLimiter = rateLimit({ windowMs: 15 * 60 * 1000, max: 10 });
// export const globalRateLimiter = rateLimit({ windowMs: 15 * 60 * 1000, max: 200 });
export {};
