/**
 * authenticate.ts — JWT verification middleware (Phase 0 stub).
 *
 * Phase 1 implementation:
 *  1. npm install jsonwebtoken @types/jsonwebtoken
 *  2. Extract Bearer token from Authorization header
 *  3. Verify with env.JWT_SECRET
 *  4. Attach req.user = { _id, role } to request
 *  5. Throw ApiError.unauthorized() on failure
 *
 * See docs/AUTH.md for the full token strategy.
 */

// Phase 1: export const authenticate = (req, res, next) => { ... };
export {};
